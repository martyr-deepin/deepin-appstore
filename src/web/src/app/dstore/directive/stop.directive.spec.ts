import { StopDirective } from './stop.directive';

describe('StopDirective', () => {
  it('should create an instance', () => {
    const directive = new StopDirective();
    expect(directive).toBeTruthy();
  });
});
